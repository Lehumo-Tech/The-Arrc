/**
 * Shared media base URLs.
 *
 * In this Prisma-only deployment all media is served from the local `/public`
 * directory, so MEDIA_BASE is empty (image paths in seed-data are already
 * root-relative, e.g. `/leaders/president.jpeg`) and VIDEO_BASE points at the
 * local `/videos` folder.
 */
export const MEDIA_BASE = "";
export const VIDEO_BASE = "/videos";
